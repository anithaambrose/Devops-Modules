# Python build tool

In Python, PIP (Python Package Installer) is the most commonly used build and package management tool, but it's important to know that PIP is a package manager, not a build tool itself. 

Let's break it down clearly:

## PIP (Python Package Installer)

  •	Used to install and manage packages from PyPI. 
  
  •	Comes pre-installed with Python versions 3.4+.
  
## Common PIP Commands:

Command                               -        Description

pip install <package>                 -        Install a package

pip uninstall <package>               -        Remove a package

pip list                              -        List installed packages

pip freeze                            -        Show installed packages with versions

pip install -r requirements.txt       -        Install packages listed in a file
 

## Python Build Tools Overview

While PIP installs packages, Python build tools help create and distribute your own packages. Popular tools include:
